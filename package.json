{
  "name": "lerna-ts-qpitlove",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap --hoist",
    "clean": "lerna run clean",
    "build": "lerna run build",
    "publish": "lerna publish --registry=https://registry.npmjs.org",
    "clean:all": "npm run clean && lerna clean -y"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
